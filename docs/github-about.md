# GitHub repo metadata

Paste these into the repo's **About** panel (⚙️ next to "About" on the repo home page).

## Description (short — the one-liner under the repo name)

> Multi-client social media scheduler for LinkedIn & Instagram — schedule, queue, and auto-publish for 15+ clients from one dashboard. Built to run on free tiers.

### Shorter alternative (if the above is too long)

> Multi-client LinkedIn & Instagram scheduler for agencies — one dashboard, auto-publishing, free-tier friendly.

## Website

> (your Vercel deployment URL, once live — e.g. https://cue.vercel.app)

## About / longer summary

Cue is a self-hostable social media scheduler built for agencies and solo social managers who run
content for many clients at once. Clients are first-class workspaces — each with its own connected
LinkedIn and Instagram accounts, brand color, queue, and calendar. Compose once, target multiple
accounts, schedule or post now, and let GitHub Actions cron handle publishing, token refresh, and a
7-day media cleanup. The entire stack (Next.js + Supabase + Cloudflare R2 + GitHub Actions + Vercel)
is designed to run on free tiers.

## Topics / tags

```
social-media-scheduler
social-media-management
linkedin
instagram
nextjs
react
typescript
prisma
supabase
postgresql
cloudflare-r2
tailwindcss
shadcn-ui
scheduler
oauth
github-actions
content-scheduling
saas
agency-tools
buffer-alternative
```

> Tip: GitHub allows up to 20 topics. The list above is exactly 20 — paste them one at a time
> (or comma-separated) into the **Topics** field.
